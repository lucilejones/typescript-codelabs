// if we want to compile all the TS files in a project at the same time
// and to configure how the TS compiler behaves
// then we add a TS compiler config file.
// in the console: npx tsc --init
// once we work with Angular we'll get the tsconfig file automatically

// there are lots of options we can set
// the most important is strict-mode: true