// primitives
// this are lowercase for the primitive type
// uppercase would point at the Number and String objects

let age: number = 24;

let userName: string;



let isInstructor: boolean;

isInstructor = true;


// in a function
function add(a: number, b: number) {
    return a + b;
}

const result = add(2, 5);

console.log(result);
